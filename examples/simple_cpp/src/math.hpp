#pragma once

int add(int a, int b);
int subtract(int a, int b);
bool is_positive(int value);
double average(double a, double b);

// Not covered by tests — candidate for pseudo-tested classification.
int unused_helper(int x);
